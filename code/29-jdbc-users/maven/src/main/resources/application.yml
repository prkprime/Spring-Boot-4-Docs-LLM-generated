spring:
  application:
    name: 29-jdbc-users
  datasource:
    url: jdbc:h2:mem:chapter29;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
