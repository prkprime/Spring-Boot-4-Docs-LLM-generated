#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "${ROOT}"

if [[ "${SKIP_FULL_VERIFY:-0}" == "1" ]]; then
  echo "SKIP_FULL_VERIFY=1 set; skipping pre-push full verification."
  exit 0
fi

scripts/verify-all.sh
